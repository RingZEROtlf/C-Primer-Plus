#include <stdio.h>

int main(void)      // a simple C program
{
    int num;        // define a variable named num
    num = 1;        // give num a value
    printf("I am a simple ");   // use printf() function
    printf("computer.\n");
    printf("My favoritet number is %d because it is first.\n", num);
    return 0;
}